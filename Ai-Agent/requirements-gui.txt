# ============================================================
# Bingo behavioral agent — desktop GUI / voice dependencies
# Pinned to the versions verified on Python 3.12 (Windows wheels).
# ============================================================
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install -r requirements-gui.txt
#
# Visual preview (python Bingo_Agent.py --preview) needs everything EXCEPT
# PyAudio. Full voice mode also needs PyAudio for the microphone.

# --- GUI + animation ---
PyQt5==5.15.11
PyQt5-Qt5==5.15.2
PyQt5-sip==12.18.0
pygame==2.6.1

# --- Voice I/O (STT/TTS via OpenAI) ---
openai==2.38.0
SpeechRecognition==3.16.1
PyAudio==0.2.14            # microphone capture (full voice mode only)
webrtcvad-wheels==2.0.14   # continuous voice-activity detection (low-latency listening); legacy recorder is the fallback

# --- Shared / integration layer ---
numpy==2.4.6
python-dotenv==1.2.2
pydantic==2.13.4
PyYAML==6.0.3
httpx==0.28.1

# --- Defensive monitoring (sniffer mode + WAF model) ---
# Sniffer mode also requires the Npcap driver (https://npcap.com) and running
# the app as Administrator. HTTP traffic is inspected; HTTPS is metadata-only.
scapy==2.7.0
xgboost==3.2.0
scikit-learn==1.8.0
pandas==3.0.3
scipy==1.17.1
