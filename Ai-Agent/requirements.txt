# ============================================================
# Bingo AI-Agent — unified Python dependencies
# ============================================================
# Covers the Offensive engine, Defensive WAF engine, the
# integration layer, and the headless behavioral entry point.
# GUI/voice-only deps (PyQt5, pygame, faster-whisper,
# speech_recognition) are intentionally optional and listed at
# the bottom — they are NOT required for headless/Docker runs.

# ---- LangChain stack (1.x; agents live in langchain-classic) ----
langchain>=1.0
langchain-classic>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langchain-community>=0.4
langchain-experimental>=0.3
langchain-text-splitters>=1.0

# ---- Vector store / RAG (hybrid: dense FAISS + sparse BM25) ----
faiss-cpu>=1.8.0
rank-bm25>=0.2.2

# ---- LLM / HTTP ----
openai>=1.50.0
httpx>=0.27.0
requests>=2.31.0

# ---- Web analysis ----
playwright>=1.40.0

# ---- Core utils ----
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
loguru>=0.7.0

# ---- Defensive WAF model (XGBoost + sklearn pipeline) ----
xgboost>=2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0

# ---- Optional: voice/GUI behavioral agent (not used headless) ----
# PyQt5>=5.15
# pygame>=2.5
# SpeechRecognition>=3.10
# faster-whisper>=1.0
