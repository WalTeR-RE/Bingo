# Normalize line endings: treat text files as text and store them as LF.
* text=auto eol=lf

# Known binaries — never normalize or diff these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pkl binary
*.pdf binary
*.woff binary
*.woff2 binary
